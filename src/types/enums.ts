export enum Role {
    ADMIN = 'ADMIN',
    LECTURER = 'LECTURER',
    STUDENT = 'STUDENT'
}

export enum ValidationStatus {
    VALID = 'VALID',
    INVALID_TOKEN = 'INVALID_TOKEN',
    INVALID_LOCATION = 'INVALID_LOCATION',
    DUPLICATE = 'DUPLICATE'
}
